# M03-T01 — P2 GraphQL-primary host-control foundation evidence

Date: 2026-09-25
Card: `M03-T01`
Plan authority: approved `planning/PASEO_GUI_RUNTIME_P2.md`

## Reconciliation from the prior blocker

The earlier credential-backed blocker evidence remains preserved at `implementation/workstreams/feature-paseo-gui-runtime/evidence/M03-T01-blocked-live-credential-2026-09-25.md`.

Approved P2 changed only the acceptance timing: authenticated least-privilege readback and reversible live GraphQL mutation proof are no longer required to complete the M03-T01 implementation foundation. They are mandatory in M06-T04 integrated acceptance before M07 production cutover.

The M03-T01 stable Card contract was therefore realigned explicitly to approved P2 before result reconciliation; the old blocker was removed from mutable Task Board state rather than bypassed.

## Exact implementation subject

- implementation commit: `43d082317339e6aef4362d2d456b41442dc739e1`
- structured client: `scripts/unraid_graphql_host_control.py`
- credential materializer: `scripts/configure_unraid_graphql_credential.py`
- permission profile: `config/unraid-host-control/permission-profile.json`
- contract tests: `tests/test_unraid_graphql_host_control_contract.py`
- operational contract: `docs/UNRAID_GRAPHQL_HOST_CONTROL.md`

A compare from `43d082317339e6aef4362d2d456b41442dc739e1` to the current workstream branch showed no later changes to those implementation files; later commits are durable planning/workflow/evidence changes.

## Exact dependency

The Card dependency remains:

`implementation/workstreams/feature-paseo-gui-runtime/results/M02-T03.md@cac4ac5e5a4b6c96ad71d9bbce12962388f7a4be:0d13d7fe5a6dd29fb68e700d13cc5c1251722c28`

Readback of that exact commit/path returns blob `0d13d7fe5a6dd29fb68e700d13cc5c1251722c28`.

## Repository/CI acceptance evidence

GitHub Actions run `36160307230` is `completed/success` on exact head SHA `43d082317339e6aef4362d2d456b41442dc739e1`.

The single `contract-build-and-smoke` job is GREEN, including:
- exact predecessor binding verification;
- candidate image/runtime contract tests, including M03-T01 host-control tests;
- frozen Paseo child-image build;
- disposable image/provenance smoke;
- disposable persistence/ownership smoke;
- disposable Pi instruction-plane smoke;
- immutable foundation metadata inspection.

The M03-T01 contract tests verify:
- no SSH implementation or Docker-socket/broad host mount dependency in this subject;
- only `start`, `stop`, `restart`, `pause`, `unpause` are constructible Docker mutations;
- permission profile has no broad role and exactly `INFO:READ_ANY`, `DOCKER:READ_ANY`, `DOCKER:UPDATE_ANY`;
- API keys are read only from private regular files, not command-line values;
- credential install/rotation is atomic, private and secret-safe;
- missing/invalid credentials and GraphQL errors fail closed with machine-readable output.

## Exact live non-secret Tower facts

Preserved live readback establishes:
- Unraid `7.2.4`;
- native `unraid-api 4.37.4+ad268301` online;
- GraphQL path `/graphql`;
- API key request header `x-api-key`;
- resolver permission mapping: `INFO:READ_ANY` for info readback, `DOCKER:READ_ANY` for Docker readback, `DOCKER:UPDATE_ANY` for start/stop/restart/pause/unpause;
- unauthenticated live `info` query returns GraphQL `UNAUTHENTICATED`, proving fail-closed endpoint behavior.

No raw API key is present in this evidence.

## Deferred mandatory acceptance

M03-T01 does **not** claim authenticated least-privilege GraphQL success.

M06-T04 must still use a dedicated least-privilege credential to prove:
1. authenticated structured INFO and DOCKER readback;
2. one safe reversible ordinary GraphQL container mutation;
3. exact pre-state/post-state/restoration readback;
4. sufficiency of the bounded permission profile without a broader role;
5. forced bounded SSH fallback plus return-to-GraphQL behavior.

If that integrated smoke is absent or RED, M06 cannot exit GREEN and M07 production cutover cannot proceed.

## Outcome

GREEN for the M03-T01 implementation-foundation acceptance surface under approved P2. Credential-backed integrated acceptance remains downstream and unclaimed.
