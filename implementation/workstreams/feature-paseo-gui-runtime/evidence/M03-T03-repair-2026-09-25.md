# M03-T03 — R01 bounded correction evidence

Date: 2026-09-25
Card: `M03-T03`
Corrected implementation subject: `elmakus/pi-unraid@a19e53e35a729820a1dc62ec98ca01e354053a5d`
Prior review: `implementation/workstreams/feature-paseo-gui-runtime/reviews/M03-T03-R01.toml`
Prior RED evidence: `implementation/workstreams/feature-paseo-gui-runtime/evidence/M03-T03-R01-independent-review-2026-09-25.md`

## Correction scope

R01 found one bounded acceptance defect: the host-control doctor did not prove the required GraphQL healthy/unreachable/auth/protocol failure matrix, and HTTP 401/403 was collapsed into a generic HTTP error surface.

The correction remains entirely inside the accepted M03-T03 Card:
- `scripts/unraid_host_control_doctor.py` now maps HTTP 401/403 to bounded `auth` status and preserves only the numeric HTTP status as additional structured metadata.
- `tests/test_unraid_host_control_doctor_contract.py` now explicitly verifies transport/unreachable, auth and protocol failures, in addition to the existing healthy path, while asserting the API key is not emitted.
- No GraphQL mutation, gated SSH execution, forced smoke-marker mutation, authority rule, policy gate or transport ownership was changed.

## Exact predecessor bindings

The unchanged Card dependencies remain:
- `implementation/workstreams/feature-paseo-gui-runtime/results/M03-T01.md@16e5a14f1f0357b8af40fa23602c6d6c1da24724:1b157d85116b5a9dffab7df7766f62cc8a288f3e`
- `implementation/workstreams/feature-paseo-gui-runtime/results/M03-T02.md@97e7833edaa55d54b35336a07c46bf261795fc46:a5c8a0c3a84b5c7f6da3d147896ee60995860800`

## CI / readback

GitHub Actions run `36192679347` on exact corrected SHA `a19e53e35a729820a1dc62ec98ca01e354053a5d` completed **success**.

The `contract-build-and-smoke` job completed GREEN for:
- exact predecessor binding verification;
- candidate/image/runtime/Relay/instruction-plane/GraphQL/host-safety/host-doctor contract suites;
- frozen Paseo child-image build;
- disposable image/provenance smoke;
- disposable persistence/ownership smoke;
- disposable Pi instruction-plane smoke;
- immutable foundation metadata inspection.

The new doctor contract coverage specifically proves:
- transport/unreachable failure -> structured RED `transport`;
- HTTP 401 -> structured RED `auth` with bounded numeric `http_status = 401`;
- protocol failure -> structured RED `protocol`;
- raw API-key material is absent from those doctor outputs.

## Outcome

The R01 finding is corrected on the exact implementation subject above. The result is ready for a new independent R02 review; this correction context is not eligible to issue that independent verdict.
