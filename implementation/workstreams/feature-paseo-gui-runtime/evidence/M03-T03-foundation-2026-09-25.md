# M03-T03 — Read-only host-control doctor implementation evidence

Date: 2026-09-25
Card: `M03-T03`
Implementation subject: `elmakus/pi-unraid@3af85a170eb0f3274ea7321003680051450350dd`

## Exact predecessors

The Card binds the accepted host-control foundations as:

- `implementation/workstreams/feature-paseo-gui-runtime/results/M03-T01.md@16e5a14f1f0357b8af40fa23602c6d6c1da24724:1b157d85116b5a9dffab7df7766f62cc8a288f3e`
- `implementation/workstreams/feature-paseo-gui-runtime/results/M03-T02.md@97e7833edaa55d54b35336a07c46bf261795fc46:a5c8a0c3a84b5c7f6da3d147896ee60995860800`

GitHub Actions run `36191535148` revalidated both exact result bindings before tests/build.

## Implemented doctor

- `scripts/unraid_host_control_doctor.py` is diagnosis-only and exposes quick/full depths.
- GraphQL is treated as the required structured primary control plane. The doctor validates the existing endpoint/private API-key contract and performs only the existing INFO/DOCKER readback query; it reports bounded capability state plus a truncated secret-safe credential fingerprint.
- SSH fallback health is optional/degraded while GraphQL remains healthy. Quick depth validates secret-safe SSH configuration metadata; full depth adds only the fixed non-mutating SSH probe. The doctor does not invoke `gated_exec` or the forced mutation smoke marker.
- The repository host-safety policy is checked for the accepted GraphQL-primary rule shape, the explicit high-impact SSH gate classes, required fallback reasons and fail-closed unknown-operation policy. Safety-policy drift is RED.
- Aggregation is machine-readable and emits a concise GREEN/WARN/RED summary. GraphQL or safety-policy failure is RED; unavailable/degraded fallback SSH is WARN when the primary control plane remains healthy.
- Doctor identity is explicitly `unraid_host_control`; it does not execute Project Workflow Recovery, Orchestration Runtime diagnosis, reconcile/update semantics or any host mutation.
- `docs/UNRAID_HOST_CONTROL_SAFETY.md` documents the read-only boundary and preserves M06-T04 as the owner of credential-backed reversible GraphQL mutation, forced bounded SSH fallback and return-to-GraphQL acceptance.

## CI / readback

GitHub Actions run `36191535148` on exact SHA `3af85a170eb0f3274ea7321003680051450350dd` completed **success**.

The single `contract-build-and-smoke` job completed GREEN for:
- exact predecessor bindings, including M03-T01 and M03-T02;
- all candidate/image/runtime/Relay/instruction-plane/GraphQL/host-safety contract tests;
- the new host-control doctor contract suite;
- frozen Paseo child-image build;
- disposable image/provenance smoke;
- disposable persistence/ownership smoke;
- disposable Pi instruction-plane smoke;
- immutable foundation metadata inspection.

The doctor contract tests mechanically verify:
- policy drift on a high-impact user gate becomes RED;
- GraphQL diagnosis does not call the mutation path and never emits the raw API key;
- GraphQL application failure maps to RED;
- full SSH diagnosis uses only the fixed probe and does not call gated execution or the mutation smoke marker;
- optional SSH degradation aggregates to WARN while core GraphQL/policy failure aggregates to RED;
- JSON state and concise human summary remain present without claiming PW Recovery or OR doctor authority.

## Deferred mandatory acceptance

M03-T03 does not claim live credential-backed mutation or fallback acceptance. Approved P2 still requires M06-T04 to prove authenticated least-privilege GraphQL readback, a safe reversible GraphQL mutation with restoration, forced bounded SSH fallback, host-doctor checks and return-to-GraphQL behavior before production cutover.

## Outcome

GREEN implementation return for the M03-T03 Card acceptance surface, ready for required independent implementation review.
