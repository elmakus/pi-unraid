# M04-T01 — R01 bounded correction evidence

Date: 2026-09-26
Card: `M04-T01`
Corrected implementation subject: `elmakus/pi-unraid@b6946c004ef39cbd1d1b55532c2a152dd3e22611`
Prior review: `implementation/workstreams/feature-paseo-gui-runtime/reviews/M04-T01-R01.toml`
Prior RED evidence: `implementation/workstreams/feature-paseo-gui-runtime/evidence/M04-T01-R01-independent-review-2026-09-26.md`

## Correction scope

R01 found one bounded acceptance defect: observation sanitization removed extra credential-shaped fields but still emitted arbitrary contents from allowed `version` and `location` fields, and unexpected observation IDs were also emitted verbatim. That did not guarantee the Card/PGR no-raw-credentials boundary.

The correction remains entirely inside the accepted M04-T01 Card:
- `scripts/environment_capability_inventory.py` now emits repository-known exact version/location strings only when the observation exactly matches desired state.
- Mismatched observed version/location strings are represented only by deterministic SHA-256 fingerprints, while health/drift classification still uses the raw in-memory observation and reports the required mismatch.
- Unexpected capability observation IDs are surfaced only by deterministic SHA-256 fingerprint, so an unknown key cannot leak credential material.
- Non-object observations remain bounded WARN/unobserved rather than raising an uncontrolled error.
- `tests/test_environment_capability_inventory_contract.py` now injects raw credential material through `version`, `location` and an unexpected observation ID and proves the secret is absent from deterministic JSON output.
- No reconcile/update/install/delete behavior, network resolution, OR policy, PW workflow state, production mutation or capability delivery behavior changed.

## Exact predecessor bindings

The unchanged Card dependencies remain:
- `implementation/workstreams/feature-paseo-gui-runtime/results/M01-T03.md@bbc862c37c0da81a08a91876499cfe865be958ab:3772f647f5a0c55e856d3ae8e3feb05c6fb3e1f5`
- `implementation/workstreams/feature-paseo-gui-runtime/results/M02-T03.md@cac4ac5e5a4b6c96ad71d9bbce12962388f7a4be:0d13d7fe5a6dd29fb68e700d13cc5c1251722c28`

## CI / readback

GitHub Actions run `36194988666` on exact corrected SHA `b6946c004ef39cbd1d1b55532c2a152dd3e22611` completed **success**.

The `contract-build-and-smoke` job completed GREEN for:
- exact predecessor binding verification;
- the complete candidate/image/runtime/Relay/instruction-plane/GraphQL/host-safety/host-doctor contract suite;
- the corrected Environment Capability Inventory contract suite;
- frozen Paseo child-image build;
- disposable image/provenance smoke;
- disposable persistence/ownership smoke;
- disposable Pi instruction-plane smoke;
- immutable foundation metadata inspection.

The corrected inventory contract specifically proves:
- exact desired observations remain literal and GREEN/no-drift;
- version mismatch remains RED `version_mismatch` while the raw mismatched value is not emitted;
- secret-bearing `version` and `location` values are replaced by deterministic fingerprints;
- a secret-bearing unexpected capability ID is surfaced by fingerprint only;
- raw credential material is absent from the normalized JSON output.

## Outcome

The R01 finding is corrected on the exact implementation subject above. The result is ready for a new independent R02 review; this correction context is not eligible to issue that independent verdict.
