# M03-T02 — R01 corrective repair evidence

Date: 2026-09-25
Card: `M03-T02`
Implementation subject: `elmakus/pi-unraid@97faf73d43283d47538f331f5a41697a35618045`
Corrects review attempt: `M03-T02-R01` (**RED**)

## Exact predecessor

The repaired Card still binds `M03-T01` as:

`implementation/workstreams/feature-paseo-gui-runtime/results/M03-T01.md@16e5a14f1f0357b8af40fa23602c6d6c1da24724:1b157d85116b5a9dffab7df7766f62cc8a288f3e`

GitHub Actions run `36189067667` revalidated that exact Git blob before tests/build.

## R01 corrections

- GraphQL one-container mutations now consume the shared `host-safety-policy.json` before the side effect. The path performs bounded container pre-readback, binds the concrete `graphql_container_<action>` operation plus container scope, evaluates the policy, requires the operation to remain an ordinary mutating GraphQL class with transport-internal pre-readback and no user gate, and only then executes the mutation. Removing or reclassifying the operation fails closed before the write.
- SSH command files now name a concrete gated operation class. The guard binds `operation class + argv` into the exact evidence scope, and the SSH executor rejects ordinary or unclassified operation classes.
- Gated SSH mutation classes no longer accept caller-controlled `rollback_applicable=false`. The accepted policy requires a GREEN rollback-anchor artifact for every gated mutation class in addition to exact-scope GREEN pre-readback and private external-user-authorization evidence. This is intentionally conservative and prevents the command payload from opting out of rollback evidence.
- The generic SSH administration class remains user-gated rather than ordinary. The explicit high-impact classes remain separate policy entries, so ordinary GraphQL actions cannot be used by the SSH executor and unknown classes fail closed.
- Documentation and contract tests were updated to the operation-bound scope and shared-policy execution contract.

## CI / readback

GitHub Actions run `36189067667` on exact SHA `97faf73d43283d47538f331f5a41697a35618045` completed **success**.

The `contract-build-and-smoke` job is GREEN for:
- exact predecessor bindings;
- candidate/image/runtime/Relay/instruction-plane/GraphQL/host-safety contract tests;
- repaired GraphQL policy-denial/reclassification tests;
- repaired SSH operation-class and rollback-anchor tests;
- frozen child-image build;
- disposable image/provenance smoke;
- disposable persistence/ownership smoke;
- disposable Pi instruction-plane smoke;
- immutable foundation metadata readback.

No credential-backed SSH or authenticated GraphQL mutation was executed against Tower. Approved P2 continues to assign the real credential-backed GraphQL readback/reversible mutation plus forced bounded SSH fallback and return-to-GraphQL acceptance to `M06-T04`.
