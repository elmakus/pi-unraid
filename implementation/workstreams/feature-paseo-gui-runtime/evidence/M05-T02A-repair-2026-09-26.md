# M05-T02A — R01 bounded correction evidence

Date: 2026-09-26
Card: `M05-T02A`
Corrected implementation subject: `elmakus/pi-unraid@d4523248b8025d65649d4b734e58e89a894e03d9`
Prior review: `implementation/workstreams/feature-paseo-gui-runtime/reviews/M05-T02A-R01.toml`
Prior RED evidence: `implementation/workstreams/feature-paseo-gui-runtime/evidence/M05-T02A-R01-independent-review-2026-09-26.md`

## Correction scope

R01 found two bounded defects inside the existing Card authority; both are
corrected with no contract change:

- RED-1: `verify_build_inputs` matched `FROM <reference>` by substring, so a
  `--context` with a wrong effective digest and the expected `FROM` only in a
  comment passed. `scripts/paseo_buildx.py` now parses effective `FROM`
  instructions (`effective_from_images`: skips full-line comments and parser
  directives, joins continuations, case-insensitive, strips `--platform` flags
  and `AS <stage>`) and rejects a missing instruction or any effective image
  not exactly equal to the frozen Paseo reference.
- RED-2: `--builder default` and `PI_UNRAID_BUILDX_BUILDER=default` reached
  the shared default cache on build and standalone prune. One
  `validate_builder_name` validator (empty, `paseo`, `*workstation*`,
  case-insensitive `default`) is now shared by `ensure_builder` and
  `cmd_prune`; `cmd_build` reuses the validated name.
- `tests/test_paseo_buildx_contract.py` adds 6 regression tests: wrong
  effective digest fails closed via `--context` with zero Docker calls;
  expected `FROM` only in comment fails closed (3 alias shapes, zero Docker
  calls); `FROM` flag/alias/case parsing acceptance; shared default-builder
  rejection; build rejects default via flag and env; standalone prune rejects
  default via flag and env with the record untouched.
- Diff on the corrected SHA touches only these two files (194 insertions,
  6 deletions). No candidate, Dockerfile, workflow, result, board, or review
  state changed.

## Exact predecessor bindings

Unchanged Card dependencies, verified both locally and in the exact-SHA CI
step `Verify exact predecessor bindings`:

- `implementation/workstreams/feature-paseo-gui-runtime/results/M05-T01-R02.md@40ad9529f7f3bb05a27caa306d21a3cdc83c5bcd:7b3855238c59f35f2cd2af05a0d1456db34832db`
- `implementation/workstreams/feature-paseo-gui-runtime/results/M01-T03.md@bbc862c37c0da81a08a91876499cfe865be958ab:3772f647f5a0c55e856d3ae8e3feb05c6fb3e1f5`

## CI / readback

[Buildx foundation CI run 36238131162](https://github.com/elmakus/pi-unraid/actions/runs/36238131162)
on exact corrected SHA completed **success** (`feat/paseo-gui-runtime`,
job `108393563010`, all steps GREEN).

- Contracts: 12 suites, 136/136 OK — Buildx 39, candidate-resolver 27,
  child-image 6, runtime 5, relay-auth 5, instruction-plane 6,
  global-capabilities 4, GraphQL host-control 9, host-control guard 10,
  host-control doctor 7, capability inventory 5, capability control 13.
  Local rerun on the repair SHA also passes 136/136.
- Cold: fresh dedicated builder `pi-unraid-paseo` (`reused: false`),
  tag `pi-unraid:paseo-b4e0c1e7c276`, image
  `sha256:c75dda8998e2a54ab246392a4191a70859614560e323f6af02a34da813107f48`,
  metadata digest `sha256:dfa06ba0b01f0b9a94e3870beabb75a0628f05ef393a56692e4d9fed87ed8d3f`,
  0 cached steps; phases resolution 23 ms, builder 366 ms, build 121924 ms,
  test 46996 ms, prune 93 ms, all `ok`.
- Warm identical: builder reused, same image ID, 8 cached steps
  (`#3,#7-#13 CACHED` in log and record), build 5136 ms.
- Warm bounded-label change: builder reused, distinct image
  `sha256:e187ac656e1f85c68d27d5500e7866e79cf4e016de19a5d8d30c67069ecb63f8`,
  8 cached steps, build 4950 ms.
- Smokes on the cold record: all 4 GREEN — image_provenance 6369 ms,
  persistence_ownership 13761 ms, instruction_plane 4332 ms,
  global_capabilities 22533 ms.
- Prune: `prune --record /tmp/cold-record.json --keep-storage 8GB` gated on
  the cold record's successful build+test, builder `pi-unraid-paseo`,
  status `ok`; `buildx du` non-empty before and after, cold image still
  inspectable with candidate label
  `sha256:b4e0c1e7c276371b84abd5c9aa7e325705b71349fe614b76855510dabf350b69`.
  Buildx notes `--keep-storage` renamed to `reserved-space`; the run still
  reports `keep_storage: 8GB` and prune `ok`.

No private GHCR, Tower, production, or legacy standalone-Pi mutation was
performed. The CI runner is ephemeral, so builder/cache reuse is proven
across separate invocations within one job; on a persistent host the same
named builder/state directory survives across jobs.

## Outcome

Both R01 blocking findings are corrected on the exact implementation subject
above, with inspected cold/warm/smoke/prune evidence. The result is ready for
a new independent review attempt; this correction context is not eligible to
issue that independent verdict.
