# M04-T02 — R01 repair evidence

Date: 2026-09-26
Card: `M04-T02`
Corrected implementation subject: `elmakus/pi-unraid@b93be404f58dbefd60a0acca9010864d8dfecda9`
Repairs: R01 finding in `implementation/workstreams/feature-paseo-gui-runtime/evidence/M04-T02-R01-independent-review-2026-09-26.md`

## Correction

The R01 finding showed that the control surface could accept non-canonical desired-state inputs and still build a canonical-looking reconcile plan.

The corrected implementation now enforces the current repository-owned authority in two layers:

- the reconcile CLI rejects a non-canonical repository root, inventory definition path or candidate path before it can derive a reconcile plan;
- the doctor/reconcile library boundary validates every input payload against a canonical authority snapshot derived from the repository-owned `config/environment-capabilities.json` and its current canonical candidate source. Candidate identity, approved capability set, delivery mode, desired state, runtime location and probe metadata must all match current canonical authority.

Observation data remains task-provided and may vary; only desired-state authority is frozen. The existing secret-safe observation normalization and unexpected-extra reporting behavior is unchanged.

## Regression coverage

`tests/test_environment_capability_control_contract.py` now proves:

- reconcile CLI rejects alternate `--root`;
- reconcile CLI rejects alternate `--definition`;
- reconcile CLI rejects alternate `--candidate`;
- a directly supplied payload rebased to an alternate candidate desired version is rejected by both doctor and reconcile before any action can be planned.

Existing plan-tamper, candidate-change, desired-state-change, unexpected-extra, secret-safety, readback and no-update/delete/OR/PW-authority tests remain intact.

## Exact CI/readback

GitHub Actions run `36198164039`:
- head SHA: `b93be404f58dbefd60a0acca9010864d8dfecda9`
- workflow: `Paseo image and runtime foundation`
- job: `contract-build-and-smoke`
- conclusion: `success`

The run completed GREEN for:
- exact predecessor binding checks, including M04-T01;
- the complete candidate/image/runtime/Relay/Pi-instruction-plane/GraphQL/host-safety/host-doctor/environment-inventory suite;
- the corrected environment capability control contract, including the new authority-source substitution regressions;
- frozen Paseo child-image build;
- disposable image/provenance smoke;
- disposable persistence/ownership smoke;
- disposable Pi instruction-plane smoke;
- immutable foundation metadata inspection.

## Boundary

The correction changes no approved capability, desired version line, update semantics, OR/PW authority, production/Tower state or host-control behavior. It only closes the authority-substitution path inside the existing M04-T02 contract.
