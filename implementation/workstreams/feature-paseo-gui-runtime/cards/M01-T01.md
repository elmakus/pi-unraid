# Task Card

- Card ID: M01-T01
- Included scope: Implement the bounded exact-candidate resolver for the approved Paseo/Pi global environment baseline and persist one frozen machine-readable candidate resolution containing accepted stable-line selections plus exact version and digest/SHA/integrity where upstream exposes them; fail closed when a mandatory component cannot be resolved or the coordinated set is known incompatible; produce durable verification evidence for the frozen candidate without building or deploying production.
- Excluded scope: Paseo child-image construction, package installation into the image, production or temporary runtime deployment, Relay/auth/persistence setup, host-control implementation, BuildKit/GHCR pipeline implementation beyond any read-only metadata lookup needed by resolution, compatibility-exception approval, and any OR or future PW extension integration.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_001_RUNTIME_SUBSTRATE.md, decisions/ADR_PGR_004_UPDATE_BUILD_ROLLBACK.md, planning/PASEO_GUI_RUNTIME_P1.md
- Dependencies: none
- Acceptance: One deterministic resolver entrypoint can resolve the currently approved stable lines for the Paseo base, Pi, Node/runtime prerequisite line, Playwright/Chromium baseline, SpecPi, pi-mcp-adapter and other versioned approved global baseline components into one frozen candidate record; the record identifies provenance and exact immutable identity where available, separates unresolved/not-applicable identity fields explicitly, is reproducible/readable by later build work, and exits non-zero without replacing the accepted candidate when any mandatory resolution or compatibility check fails.
- Required tests/readback: Run resolver in a clean/read-only resolution flow; validate schema/required component coverage; verify the Paseo base resolves to an exact official ghcr.io/getpaseo/paseo stable image identity; verify repeated resolution of unchanged upstream state is semantically identical; exercise at least one mandatory-resolution failure fixture/path and confirm fail-closed behavior; inspect the frozen record for raw-secret absence and exact provenance fields.
- Review requirement: required
- Technical contract: none
