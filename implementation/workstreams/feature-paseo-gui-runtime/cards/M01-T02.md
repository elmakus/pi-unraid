# Task Card

- Card ID: M01-T02
- Included scope: Replace the legacy standalone Node image foundation with the exact frozen official Paseo image ghcr.io/getpaseo/paseo@sha256:d413ff361bc4018d559da3d517a6d5a9eaca721fbb1b71ae8df3dcf7a965c136; install the frozen Pi 0.87.1, Playwright 1.63.0 browser runtime, GitHub CLI 2.101.0, Docker CLI 29.8.1, Docker Compose 5.5.1 and the approved general shell/Git/network/build/Python baseline needed for development; provide server-side Chromium plus headed Xvfb support; preserve Paseo HOME=/home/paseo, upstream paseo non-root identity and upstream entrypoint/server behavior; add deterministic repository verification that the Dockerfile consumes the frozen candidate rather than re-resolving latest.
- Excluded scope: SpecPi or pi-mcp-adapter delivery/configuration, Relay/pairing/auth setup, production or temporary runtime deployment, Compose persistence/ownership redesign, host-control, GHCR publication/BuildKit cache pipeline, update/cutover/rollback implementation, production acceptance, OR integration, and future PW extension integration.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_001_RUNTIME_SUBSTRATE.md, decisions/ADR_PGR_004_UPDATE_BUILD_ROLLBACK.md, planning/PASEO_GUI_RUNTIME_P1.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M01-T01.md@dd3047320faf92f0dfe9d0ac0a0f5ff590e11250:4ccca450fa664344ef3ded7de33de7df7c79d8f9
- Acceptance: Dockerfile derives from the exact official Paseo digest frozen by M01-T01, keeps the upstream paseo user/HOME/entrypoint contract, installs only identities frozen by the accepted candidate for independently versioned tools, provides Pi plus practical development tooling and both headless Chromium and headed Xvfb prerequisites, and has a deterministic validation path that fails when the Dockerfile drifts from the frozen candidate or silently reintroduces a floating base/tool version.
- Required tests/readback: Validate the exact predecessor candidate/result binding; run repository tests that compare Dockerfile build arguments and package identities to config/paseo-candidate.json; verify no legacy node: base, /home/pi user rewrite, NOPASSWD sudo contract or legacy pi-unraid entrypoint remains in the child-image foundation; verify the upstream Paseo ENTRYPOINT is preserved; build the child image in CI if runner capability permits and otherwise perform syntax/static contract validation while leaving image-level executable smoke to M01-T03; verify Playwright/Chromium and Xvfb installation commands are present and candidate-driven.
- Review requirement: required
- Technical contract: none
