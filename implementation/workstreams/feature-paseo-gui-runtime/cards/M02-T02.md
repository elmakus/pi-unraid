# Task Card

- Card ID: M02-T02
- Included scope: Consume the exact GREEN M02-T01 persistence/ownership result and implement a non-production Paseo Relay, pairing, authentication and secret-state contract on the frozen Paseo child image. Preserve the native persistent `/home/paseo` / `PASEO_HOME=/home/paseo/.paseo` surfaces, keep Relay opt-in and first-time account/device authentication as deliberate human actions, make already-approved session/pairing state reusable across routine container recreation, provide bounded revocation/readback where upstream supports it, and materialize credential-bearing state only through native HOME/secret mechanisms outside Git. Add deterministic disposable validation that does not require real user credentials or a production Relay/mobile cutover.
- Excluded scope: Production/Tower deployment or persistent temporary deployment, opening/publishing raw Paseo port 6767, alternate LAN/tunnel remote access, real user credential capture in Git/CI, actual phone Relay acceptance, SpecPi or pi-mcp-adapter delivery/configuration, instruction-plane/global AGENTS/skills work, Unraid GraphQL/SSH host-control, GHCR/cache/update/cutover/rollback work, OR integration, future PW-extension integration, destructive HOME migration, and high-impact host operations.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_001_RUNTIME_SUBSTRATE.md, decisions/ADR_PGR_002_AUTHORITY_BOUNDARIES.md, research/PASEO_GUI_RUNTIME_DEFINITION_FACTS_R1.md, planning/PASEO_GUI_RUNTIME_P1.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M02-T01.md@727b6f25a3b957551d10c9c24d9e9d1e26efd6ab:e5075d474bb7aaf0583baf66757add56fb556253
- Acceptance: The repository defines one upstream-compatible Paseo Relay/auth/secret-state setup and readback path that uses the frozen M02 runtime contract, persists native Relay/pairing/session state under the protected Paseo HOME, never commits raw credentials, does not publish a raw Paseo host port, keeps first-time authentication/pairing explicitly human-initiated, and supports safe reuse plus bounded revocation/readback where upstream exposes it. Disposable tests prove the non-secret configuration/state boundary survives container recreation and cleanup without production mutation; real mobile/Relay end-to-end acceptance remains deferred to M06/M07.
- Required tests/readback: Revalidate the exact M02-T01 result binding and frozen candidate identity; verify upstream Paseo 0.9.2 Relay/config/auth interfaces used by the implementation; reject host port publication, plaintext credential values in tracked repository/config fixtures and direct provider-secret environment wiring; verify secret-bearing/native auth state is rooted under the persisted `/home/paseo` contract and repository helpers accept secret material only at runtime rather than embedding it; exercise disposable HOME/config recreation with non-secret fixtures and inspect resulting permissions/ownership; verify opt-in/first-time auth semantics are not auto-triggered by normal container startup; verify bounded readback/revocation commands or capability detection fail closed when unsupported; confirm cleanup leaves no deployed runtime.
- Review requirement: required
- Technical contract: none
