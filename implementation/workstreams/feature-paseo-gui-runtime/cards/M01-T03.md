# Task Card

- Card ID: M01-T03
- Included scope: Consume the exact reviewed M01-T02 child-image foundation and add deterministic disposable image-level/provenance smoke for that exact frozen candidate: verify Pi executable/version and a provider-free Pi RPC control round-trip, server-side Playwright/Chromium in both headless and headed Xvfb modes, the required baseline development tools, inherited Paseo default server/health behavior, immutable candidate/image metadata, and absence of raw secret material in image configuration/history. The smoke may start only short-lived disposable CI containers and must leave no deployed runtime behind.
- Excluded scope: Production deployment, persistent temporary runtime deployment, Compose persistence/ownership redesign, Relay/pairing/auth configuration, SpecPi or pi-mcp-adapter installation/configuration, Unraid host-control, GHCR publication/BuildKit cache pipeline, update/cutover/rollback implementation, OR integration, future PW extension integration, and any provider/model authentication or model inference.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_001_RUNTIME_SUBSTRATE.md, decisions/ADR_PGR_004_UPDATE_BUILD_ROLLBACK.md, planning/PASEO_GUI_RUNTIME_P1.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M01-T02.md@4e60c855a84b2e74d50de812db1ba9cc156af400:be820f52901a9d2d4a40b71c3da6d19ffc08edea
- Acceptance: The exact frozen Paseo child image is reproducibly built and disposable smoke proves its immutable candidate identity/provenance, Pi 0.87.1 executable plus provider-free RPC control availability, Playwright 1.63.0 Chromium launch in both headless and headed/Xvfb modes, required baseline tools, and inherited Paseo default server/health behavior without raw secret material or production mutation. All disposable containers/processes are cleaned up after the smoke.
- Required tests/readback: Revalidate the exact M01-T02 result binding and existing candidate/child-image contract suites; build the child image from the frozen candidate; inspect image ID, candidate/version labels, HOME=/home/paseo, WORKDIR=/workspace, inherited entrypoint and healthcheck; run Pi version and a JSONL RPC get_state round-trip without provider/model work; launch Chromium once headless and once headed under Xvfb; verify gh, git, Python, Docker CLI/Compose, Playwright and Xvfb availability; start the default Paseo daemon in a disposable container, poll its /api/health endpoint to GREEN, then remove it; scan image config/history for high-confidence raw credential/private-key patterns; record the tested image ID and candidate ID.
- Review requirement: required
- Technical contract: none
