# Task Card

- Card ID: M04-T02
- Included scope: Consume the exact GREEN M04-T01 Environment Capability Inventory result and implement the repository-owned environment doctor/reconcile control surfaces under approved plan P2. Doctor must remain strictly read-only, expose bounded quick and full depths, derive health/drift from the accepted Environment Capability Inventory and emit deterministic machine-readable output plus a concise GREEN/WARN/RED human summary. Reconcile may restore only the current already-approved desired state represented by the frozen inventory/candidate and must fail closed rather than inventing a new version line, approving a new durable capability, or deleting an unexpected extra capability. Keep reconcile semantically distinct from update: update intentionally changes accepted version lines and remains owned by the later coordinated update pipeline. Preserve pi-unraid environment-availability ownership without duplicating OR runtime-use policy or PW workflow authority.
- Excluded scope: Resolving or promoting newer component versions; changing accepted version lines; approving/removing durable global capabilities; silently deleting unexpected extra capabilities; SpecPi/pi-mcp-adapter installation or compatibility delivery owned by M04-T03; M05 coordinated latest resolver/build/cutover/rollback; production/Tower deployment or mutation; host-control GraphQL/SSH mutation; raw credentials in repository/logs/evidence; OR runtime-use policy; PW workflow state; future PW extension work.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_002_AUTHORITY_BOUNDARIES.md, decisions/ADR_PGR_004_UPDATE_BUILD_ROLLBACK.md, planning/PASEO_GUI_RUNTIME_P2.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M04-T01.md@affd83590ec534ed144b9048a2b9075434761f27:5173447ec3ab8775450a9defbf193aede63ca290
- Acceptance: One repository-owned capability doctor/reconcile surface exists on top of the accepted inventory. Quick doctor performs the bounded core checks needed for routine readiness; full doctor performs the complete accepted capability observation matrix; both are read-only, deterministic for the same inputs, secret-safe, machine-readable and accompanied by concise GREEN/WARN/RED summaries. Core required capability failure is RED; optional/unobserved degraded state may be WARN where accepted authority permits it. Reconcile acts only on missing/mismatched already-approved desired state, cannot change desired version/capability approval, reports unexpected extras without deletion, and emits bounded pre/post/readback status. The implementation maintains a hard semantic separation between doctor, reconcile and later update and creates no OR/PW authority store.
- Required tests/readback: Revalidate the exact M04-T01 result binding; verify quick/full doctor paths perform no subprocess/network/install/delete/update mutation themselves and are deterministic/secret-safe; verify machine-readable plus concise GREEN/WARN/RED aggregation for exact, missing, mismatched, unobserved and unexpected observations; verify unexpected extras are reported but never selected for deletion; verify reconcile refuses unknown capability IDs, new/removal requests and desired-version changes, operates only against the frozen current desired state, and requires bounded post-action readback before claiming restoration; verify update/version-line mutation remains unavailable from this surface; verify no OR role/action/bundle/assignment policy or PW state is introduced; run the complete existing candidate/image/runtime/Relay/instruction-plane/GraphQL/host-safety/host-doctor/environment-inventory contract suite plus disposable image/provenance, persistence/ownership and Pi instruction-plane smokes on the exact implementation SHA. No production/Tower mutation is required.
- Review requirement: required
- Technical contract: none
