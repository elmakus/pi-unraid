# Task Card

- Card ID: M05-T02B
- Included scope: Extend the GREEN M05-T02A local Buildx/BuildKit foundation onto Tower as the required local-first build/cache/retention path. Establish a dedicated isolated `pi-unraid-paseo` Buildx/BuildKit builder plus host-persistent state/cache under the accepted pi-unraid appdata boundary, independent of the Paseo/legacy runtime container and of `chatgpt-ce-workstation`. Add bounded local immutable Paseo child-image retention/protection so staged promotion/rollback can preserve an explicit known-good set, and make cleanup/prune post-success-only and incapable of deleting that protected rollback set. Prove the Tower builder/cache survives separate invocations and ordinary disposable runtime replacement, and record enough machine-readable readback to recover/rebuild from canonical Git plus the frozen candidate if local Docker/BuildKit state is lost.
- Excluded scope: Private project GHCR publication/authentication, registry-backed cache/recovery, self-hosted GitHub runner installation, production Paseo cutover, changing the active production alias, post-deploy rollback execution, destructive HOME restore, legacy standalone-Pi retirement, whole-host reboot, Docker-engine restart, OS/network broad changes, OR deployment/runtime-use policy, and any fresh latest-resolution during build. M05-T03 owns staged promotion/cutover/rollback.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_004_UPDATE_BUILD_ROLLBACK.md, planning/PASEO_GUI_RUNTIME_P3.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M05-T02A-R02.md@d6643ffcdf202add37a39bc7653d6192cde36637:4273708d8f1cc8fb5eb33b3a7582fd0b21e143f4
- Acceptance: Tower exposes one dedicated `pi-unraid-paseo` docker-container builder that is not `default`, not the Workstation builder and not a runtime service. Its Buildx configuration/state and portable cache use persistent host-backed locations under the pi-unraid appdata boundary or an equivalently accepted persistent Tower path, with the exact paths reported in readback. A frozen P3 candidate build can run on Tower without resolving latest, emits immutable image/provenance/timing identity, and a later separate invocation reuses real cache. Replacing/removing a disposable runtime container does not remove the builder/cache or retained immutable image. Local image retention is bounded and explicit; cleanup after successful work preserves every protected known-good image and the active rollback set, while a failed build/smoke performs no prune or retention deletion. No GHCR/project-registry or self-hosted-runner prerequisite is introduced. Loss-of-local-state recovery is documented as deterministic rebuild from canonical Git plus the frozen candidate/provenance rather than an invented remote cache.
- Required tests/readback: Revalidate the exact M05-T02A result binding and the current P3/ADR/requirements authority. Before mutation, read back Tower Docker/Buildx state and persistent paths. Run local contract/unit tests for isolated builder naming, persistent-path selection, retention-set protection, bounded cleanup, fail-closed candidate mismatch and no registry/runner dependency. On Tower create/reuse only the bounded dedicated builder/cache state, run an exact frozen-candidate build with smoke and machine-readable record, then a separate representative warm invocation and inspect cache-hit evidence. Replace/remove only a disposable runtime probe and confirm the builder/cache/image remain. Exercise bounded post-success prune/retention logic against non-protected disposable data and prove protected immutable images remain inspectable. Finish with the existing candidate/image/runtime/Relay/instruction-plane/GraphQL/host-safety/host-doctor/inventory/control test surface applicable to this implementation SHA. Do not reboot the host or restart the Docker engine.
- Review requirement: required
- Technical contract: none
