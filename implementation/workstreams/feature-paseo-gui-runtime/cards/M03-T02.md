# Task Card

- Card ID: M03-T02
- Included scope: Consume the exact GREEN M03-T01 GraphQL-primary result and implement the repository-owned SSH fallback plus host-mutation safety guard for the accepted Paseo/Pi environment under approved plan P2. Preserve GraphQL as the normal structured primary transport and use non-interactive SSH only for API gaps, API outage, OS/plugin/filesystem/recovery work, or an explicitly forced bounded fallback test. Define private SSH credential/config handling outside Git, machine-readable transport/fallback reasons and readback, a bounded operation-classification contract, mandatory pre-mutation readback, rollback/snapshot-anchor metadata where technically applicable, and fail-closed enforcement of the accepted high-impact user gates. Keep normal Pi/Paseo non-root/no-sudo and return to GraphQL after fallback when the primary path is healthy.
- Excluded scope: Credential-backed integrated SSH fallback exercise against Tower, which approved P2 assigns to M06-T04 together with authenticated GraphQL acceptance; host-control doctor owned by M03-T03; production Paseo deployment/cutover; raw SSH/API credentials or private keys in Git/logs/evidence; interactive password SSH; broad arbitrary shell execution as an ordinary operation; Docker-socket mounting; silently authorizing whole-host reboot, whole Docker-engine restart, Unraid OS upgrade, disk formatting, broad share/appdata deletion, broad routing/firewall/default-gateway/DNS changes or comparable high-impact operations; destructive rollback; OR integration; and future PW extension work.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_002_AUTHORITY_BOUNDARIES.md, decisions/ADR_PGR_003_UNRAID_ADMIN_SAFETY.md, planning/PASEO_GUI_RUNTIME_P2.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M03-T01.md@16e5a14f1f0357b8af40fa23602c6d6c1da24724:1b157d85116b5a9dffab7df7766f62cc8a288f3e
- Acceptance: The repository defines one GraphQL-primary/SSH-fallback host-control safety plane whose fallback transport is non-interactive, credential material remains outside Git, fallback is limited to explicit bounded reasons, and machine-readable output identifies which transport/reason was used. Host mutation requests are classified before execution; every permitted mutation requires bounded pre-state readback; rollback/snapshot anchors are required where the accepted operation class makes them technically applicable; safe unambiguous rollback may be automatic; and whole-host reboot, whole Docker-engine restart, Unraid OS upgrade, disk formatting, broad share/appdata deletion, broad network-routing/firewall/default-gateway/DNS mutation and comparable high-impact actions fail closed unless an explicit external user-authorization gate is present. The implementation does not create a second PW/OR authority and automatically prefers GraphQL again once healthy.
- Required tests/readback: Revalidate the exact M03-T01 result binding and its GraphQL endpoint/permission/allowlist contract; statically reject raw credentials, interactive password SSH, Docker-socket/broad host mounts and unclassified arbitrary shell mutation; verify private SSH identity/config file checks and secret-safe status/fingerprint output; verify transport selection uses GraphQL by default, falls back only on bounded gap/outage/recovery/forced-test reasons, records the reason machine-readably and returns to GraphQL when healthy; verify ordinary operations cannot mutate without successful pre-readback; verify high-impact classes are denied without explicit external user-authorization evidence and cannot be smuggled through an ordinary command path; verify rollback/snapshot-anchor requirements and safe rollback behavior mechanically; preserve an explicit downstream obligation that M06-T04 performs authenticated GraphQL plus forced bounded SSH-fallback integration smoke and return-to-GraphQL behavior before M07.
- Review requirement: required
- Technical contract: none
