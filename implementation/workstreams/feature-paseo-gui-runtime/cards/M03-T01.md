# Task Card

- Card ID: M03-T01
- Included scope: Consume the exact GREEN M02-T03 result and implement the GraphQL-primary Unraid host-control foundation for the accepted Paseo/Pi environment. Provide a repository-owned structured client/helper and configuration contract for the native Unraid GraphQL API, materialize a dedicated host-control credential only through secret-bearing runtime/host state outside Git, select and document the least-practical Unraid API role/permission set that still supports the accepted ordinary administration surface, and provide structured health/system/container readback plus a narrowly bounded ordinary mutation path with exact pre-write and post-write readback. Keep normal Pi/Paseo non-root/no-sudo and keep the derived host inventory observational rather than canonical.
- Excluded scope: SSH fallback or failover implementation, broad mutation-guard/rollback orchestration owned by M03-T02, host-control doctor owned by M03-T03, production Paseo deployment/cutover, raw credential values in Git/logs/evidence, Docker-socket mounting into Paseo, whole-host reboot, whole Docker-engine restart, Unraid OS upgrade, disk formatting, broad share/appdata deletion, broad routing/firewall/default-gateway/DNS changes, destructive storage operations, OR integration, and future PW extension work.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_002_AUTHORITY_BOUNDARIES.md, decisions/ADR_PGR_003_UNRAID_ADMIN_SAFETY.md, research/PASEO_GUI_RUNTIME_DEFINITION_FACTS_R1.md, planning/PASEO_GUI_RUNTIME_P1.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M02-T03.md@cac4ac5e5a4b6c96ad71d9bbce12962388f7a4be:0d13d7fe5a6dd29fb68e700d13cc5c1251722c28
- Acceptance: The repository defines one GraphQL-primary Unraid host-control path that uses a dedicated credential kept outside Git, has a bounded documented permission profile, performs machine-readable structured readback before trusting or mutating host state, supports at least one safe reversible ordinary GraphQL mutation with post-write readback, and does not rely on SSH, root/sudo inside Paseo, Docker-socket exposure, runtime/session state as authority, or any high-impact operation. Exact live/readback evidence identifies the Unraid/API version and proves the credential can perform the required read surface plus the selected bounded mutation without exposing the credential.
- Required tests/readback: Revalidate the exact M02-T03 result binding; read back the current Unraid native API/version/schema and credential-management surface rather than guessing it; statically reject committed secret material, broad host mounts/Docker socket, SSH fallback implementation and high-impact mutation commands; verify client failure is closed and machine-readable on missing/invalid credentials or GraphQL errors; prove the least-practical permission profile against the required queries/mutation; exercise the selected bounded reversible mutation only after exact pre-state readback, then verify expected post-state and restore/read back the original state when applicable; ensure evidence contains only redacted/fingerprint metadata and confirm no gated high-impact action was attempted.
- Review requirement: required
- Technical contract: none
