# Task Card

- Card ID: M02-T01
- Included scope: Consume the exact GREEN M01-T03 candidate-image result and replace the legacy standalone-Pi Compose/runtime persistence shape with a non-production Paseo child-image runtime contract. Persist the complete native `/home/paseo` HOME as sensitive state, keep canonical project/worktree storage outside HOME, point Paseo's supported worktree root at the external worktree mount, provide the accepted 1 GiB shared-memory target, bounded logs and no arbitrary CPU/RAM limits, and implement an upstream-compatible ownership strategy that preserves Paseo's native entrypoint/non-root runtime contract while producing Unraid-compatible host ownership (initial target 99:100). Add deterministic disposable read/write/recreate smoke for HOME, project and worktree mounts without deploying production.
- Excluded scope: Production cutover or persistent temporary deployment on Tower, Relay/pairing/authentication or credential materialization, alternate remote-access paths, SpecPi or pi-mcp-adapter delivery/configuration, Unraid GraphQL/SSH host-control, GHCR publication/BuildKit cache pipeline, coordinated update/cutover/rollback implementation, OR integration, future PW extension integration, destructive migration of legacy Phase-1 HOME/appdata, and any high-impact host operation.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_001_RUNTIME_SUBSTRATE.md, decisions/ADR_PGR_002_AUTHORITY_BOUNDARIES.md, planning/PASEO_GUI_RUNTIME_P1.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M01-T03.md@bbc862c37c0da81a08a91876499cfe865be958ab:3772f647f5a0c55e856d3ae8e3feb05c6fb3e1f5
- Acceptance: The repository defines one Paseo child-image Compose/runtime shape that preserves the exact M01 candidate contract and upstream Paseo entrypoint/non-root behavior; binds the whole `/home/paseo` plus external project/worktree roots with host-path creation kept explicit; configures Paseo's worktree root outside HOME; uses at least 1 GiB shared memory, bounded json-file logs and no CPU/RAM caps; and a disposable Linux/Docker smoke proves read/write persistence across container recreation with newly created host-visible files owned by numeric 99:100 while the normal Paseo process remains non-root. No production container or persistent Tower path is mutated.
- Required tests/readback: Revalidate the exact M01-T03 result binding and candidate/image identity; validate Compose rendering and reject the legacy standalone `pi` service contract, `/home/pi`, old Pi UID/GID bootstrap, legacy pi-unraid health/service entrypoint and direct secret/provider wiring; verify only the accepted HOME/project/worktree bind targets, `create_host_path: false`, 1 GiB shared memory, bounded logs and absence of arbitrary resource caps; verify the exact supported Paseo worktree-root configuration is outside HOME; build/use the frozen child image and run a disposable Compose/container fixture against temporary host directories prepared for numeric 99:100, write markers as the normal runtime identity, recreate the container, read the markers back and confirm host-side numeric ownership remains 99:100; confirm cleanup leaves no deployed runtime.
- Review requirement: required
- Technical contract: none
