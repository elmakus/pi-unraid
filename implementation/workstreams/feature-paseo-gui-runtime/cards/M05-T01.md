# Task Card

- Card ID: M05-T01
- Included scope: Consume the exact GREEN M04-T03 global capability delivery result and implement the coordinated latest-stable resolver for an authorized environment-maintenance candidate. Resolve the complete approved global component set together from explicit accepted stable lines; keep project-local dependencies governed by their own manifests/lockfiles outside blanket updates. Freeze one immutable, machine-readable candidate with exact versions and available digest/SHA/integrity, source/provenance, stable-line rationale and any separately approved compatibility exception plus its recheck status. Preserve the currently accepted candidate until the staged update path explicitly promotes a fully verified new one. Expose a bounded, secret-safe resolution/readback that distinguishes resolution, reconcile and doctor from update.
- Excluded scope: New or removed global capability approval; approving a compatibility exception or silently lagging an approved component; resolving prerelease/nightly channels; independently resolving latest during image build; modifying project-local lockfiles; Buildx/GHCR/cache construction; cutover, production/Tower mutation or rollback; OR runtime-use policy; M06-M08 integrated and production acceptance.
- Authority refs: requirements/PASEO_GUI_RUNTIME.md, decisions/ADR_PGR_004_UPDATE_BUILD_ROLLBACK.md, planning/PASEO_GUI_RUNTIME_P2.md
- Dependencies: implementation/workstreams/feature-paseo-gui-runtime/results/M04-T03.md@fb6195831d047ede2f359146b69ede2690e84ef3:e31658fda900d748010bedc6caa0bb3aacf8c430
- Acceptance: One deterministic update-resolution entrypoint covers every currently approved versioned global component and its accepted stable line in one candidate; an unresolved mandatory component, known incompatible coordinated set, unapproved compatibility lag, or changed capability set fails closed without replacing the accepted candidate or emitting a promotable partial candidate. The exact frozen output is independently readable by later build work, contains provenance and immutable identity wherever upstream provides it, and never includes secrets. A proposed compatibility exception is not applied without explicit user authority; if approved later, its exact scope, rationale and automatic re-evaluation on subsequent maintenance are durable. Repeated resolution of the same source facts is semantically identical. Builds remain consumers of a frozen resolution and do not query latest. Project-local locked dependencies remain untouched.
- Required tests/readback: Revalidate the exact GREEN M04-T03 result binding and current approved capability inventory. Exercise deterministic fixtures for all approved component types and stable-line selection, including derived components, exact provenance and candidate identity; verify repeatability and rejection of missing mandatory sources, prerelease/channel substitution, incompatible combinations, unapproved exception/lag and partial writes while preserving the accepted candidate. Verify any approved exception record is re-evaluated on a later run. Check that local manifests/lockfiles are unchanged, output and failure messages are secret-safe, and build inputs consume only the frozen candidate. Run a read-only live resolution against current upstream facts when available, without adopting or promoting it, and record any source-access limitation. Run the existing candidate/resolver/doctor/capability contract suite after changes.
- Review requirement: required
- Technical contract: none
