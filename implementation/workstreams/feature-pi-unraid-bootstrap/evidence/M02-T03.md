# M02-T03 execution evidence

- Card: `M02-T03`
- Exact implementation subject: `f45bac572f139fa02dd7ddd04592409023501cfa`
- Branch: `feat/pi-unraid-bootstrap`
- Date: 2026-09-23
- Review requirement: `REQUIRED`

## Implemented result

The exact subject adds the host-operated deployment transaction required by M02:

- `scripts/update.sh`
  - serializes host updates with an external lock;
  - requires a clean tracked checkout with an upstream;
  - records pre-update source HEAD, active image ID and rendered Compose config before network/repository mutation;
  - fetches and accepts only fast-forward source movement;
  - builds a separate candidate image;
  - runs the full repository candidate verification gate before cutover;
  - retains the previous known-working image/config externally;
  - recreates the service only after candidate verification;
  - accepts deployment only after Compose health becomes GREEN;
  - automatically restores retained image/config after unhealthy cutover;
  - supports explicit rollback without rewriting Git source;
  - keeps deployment transaction retention bounded (default two);
  - never restores or rewrites persistent home/projects/worktrees.
- `scripts/verify-update-rollback.sh`
  - uses unique disposable Compose project names, tags, local Git remote/checkout and `/tmp` state only;
  - exercises successful fast-forward/candidate/cutover, explicit offline rollback, pre-cutover build failure and unhealthy post-cutover automatic rollback;
  - verifies data hashes/ownership across all three persistent bind sources;
  - verifies the persistent runtime failed-candidate marker survives deployment rollback;
  - verifies bounded Docker log rotation and final healthy deployment;
  - verifies retained transaction count stays bounded.
- `docs/OPERATOR.md`
  - documents the one normal user-facing update operation and explicit deployment rollback;
  - explicitly separates image/deployment rollback from Git source recovery;
  - states that live production cutover remains M03-authorized work.
- `openspec/changes/m02-runtime-lifecycle/tasks.md`
  - reconciles M02-T02/T03 implementation status while leaving final M02 archive/checkpoint work open.

## Full integrated Tower verification

Final exact implementation content was exercised on Tower before being frozen as the GitHub subject.

Command:

`bash scripts/verify-update-rollback.sh pi-unraid:m02-t02-r2`

Result:

**GREEN**, process exit code 0.

Observed final identities:

- previous/starting image: `sha256:3ad9ef172eefd26be1a7a0ba139153478d943a129735d31bdf4a1df1750f696e`
- successful candidate image: `sha256:8cc449b04f4de729e394f20c98effe8e1931446103738ea760f58205221c531a`
- retained transaction directories after the failure/success sequence: `2`

The candidate gate itself independently re-ran and passed:

- base image/tool inventory — GREEN;
- M01 Compose foundation — GREEN;
- M01 Git/worktree/operator foundation — GREEN;
- M02-T01 runtime selector — GREEN, real stable Pi `0.87.1`;
- M02-T02 managed lifecycle — GREEN, bounded escalation observed at `3306 ms` for the 3-second fixture grace.

T03 scenarios proven by the final run:

1. disposable source advances by fast-forward only;
2. candidate builds separately and is fully verified before active-tag cutover;
3. successful cutover records distinct pre/post source and image identities and reaches healthy state;
4. explicit rollback restores the retained previous image/config while the deployment network remains internal/offline;
5. injected pre-cutover build failure leaves the active deployment unchanged;
6. deliberately unhealthy post-cutover candidate automatically restores the prior image/config and healthy state;
7. home/projects/worktrees fixture data hashes and ownership remain unchanged throughout;
8. the existing runtime failed-candidate marker remains present/unchanged through deployment rollback;
9. restored deployment retains bounded Docker log settings (`10m`, `3`);
10. retained transaction state is bounded at two directories.

## Syntax/source checks

- `git diff --check` — GREEN
- `bash -n scripts/update.sh` — GREEN
- `bash -n scripts/verify-update-rollback.sh` — GREEN

## Scope / safety readback

- No live `pi-unraid` production service was updated, recreated or rolled back.
- No canonical `/mnt/user/appdata/pi-unraid/home`, `/mnt/user/projects` or `/mnt/user/pi-worktrees` paths were used by the verification.
- No OAuth/provider credential or live GitHub credential was consumed by the deployment fixture.
- No Docker socket or host-root mount was introduced into the Pi container.
- Only uniquely named disposable Docker resources and temporary filesystem state were mutated.
- Live on-Unraid deployment/cutover acceptance remains an M03 authorization/readiness obligation.

## Review boundary

M02-T03 requires independent review. This implementing session must not issue the verdict for subject `f45bac572f139fa02dd7ddd04592409023501cfa`.
