# M02-T03 execution evidence

- Card: `M02-T03`
- Exact implementation subject: `340121090f4b78ef3d2a3c1377d8bf5477026835`
- Previous RED subject: `f45bac572f139fa02dd7ddd04592409023501cfa`
- Previous RED evidence: `implementation/workstreams/feature-pi-unraid-bootstrap/evidence/M02-T03-review.md`
- Branch: `feat/pi-unraid-bootstrap`
- Date: 2026-09-23
- Review requirement: `REQUIRED`

## Implemented result

The corrected exact subject provides the M02 host-operated deployment transaction and closes the bounded findings from the first independent review.

- `scripts/update.sh`
  - serializes host updates with an external lock;
  - requires a clean tracked checkout with an upstream and accepts only fast-forward source movement;
  - records pre-update source HEAD, rendered Compose config and the image identity of the **actually running healthy Compose service**, rather than trusting the mutable active tag;
  - builds and verifies a separate candidate before active-tag cutover;
  - retains the exact previous known-working image/config externally;
  - treats Compose health **and** runtime identity/readback as one deployment-acceptance gate;
  - automatically restores retained image/config when either post-cutover health or runtime readback fails;
  - supports explicit rollback from local retained artifacts without rewriting Git source;
  - keeps deployment transaction retention bounded (default two);
  - never restores or rewrites persistent home/projects/worktrees.
- `scripts/verify-update-rollback.sh`
  - constructs its disposable update branch from the exact current `HEAD`, so the verifier is reproducible from a detached immutable subject and does not depend on the workstream branch continuing to exist;
  - uses unique disposable Compose project names, image tags, Git remote/checkout and `/tmp` state;
  - exercises active-tag drift versus the actually running deployment;
  - exercises successful fast-forward/candidate/cutover, explicit offline rollback, pre-cutover build failure, post-health runtime-readback failure with automatic rollback, and unhealthy post-cutover automatic rollback;
  - verifies data hashes/ownership across all three persistent bind sources and the persistent runtime failed-candidate marker;
  - verifies bounded Docker log rotation, final healthy recovery and bounded transaction retention.

## RED finding closure

### R1 — current deployed image identity

Closed by deriving `pre-image-id` from the current healthy Compose service container and verifying the underlying local image exists. The final fixture deliberately retags the active tag to a different image while the original container keeps running, then proves the transaction snapshots the running container image rather than the drifted tag.

### R2 — post-cutover runtime readback failure

Closed by making runtime readback part of the cutover acceptance gate. A deterministic post-health readback failure now invokes the same retained-image/config rollback path as an unhealthy candidate, and the fixture proves the previous deployment returns healthy.

### R3 — immutable-subject reproducibility

Closed by creating a synthetic temporary update branch from the verifier checkout's current `HEAD` inside the disposable bare remote. The final integrated verification was launched from a detached checkout of the exact corrected subject and completed successfully.

## Final exact-subject Tower verification

The corrected source was cloned from GitHub, checked out detached at exact subject `340121090f4b78ef3d2a3c1377d8bf5477026835`, and verified without using live project paths or credentials.

Checks:

- `git diff 7152b99f7e4844d3a2328aeffc0a0f4d099891c0 340121090f4b78ef3d2a3c1377d8bf5477026835 --check` — GREEN
- `bash -n scripts/update.sh` — GREEN
- `bash -n scripts/verify-update-rollback.sh` — GREEN
- `bash scripts/verify-update-rollback.sh pi-unraid:m02-t02-r2` — GREEN, process exit code 0

Observed final identities/readback:

- previous/starting image: `sha256:3ad9ef172eefd26be1a7a0ba139153478d943a129735d31bdf4a1df1750f696e`
- successful candidate image: `sha256:8cc449b04f4de729e394f20c98effe8e1931446103738ea760f58205221c531a`
- inherited M02-T01 real stable Pi: `0.87.1`
- inherited M02-T02 bounded escalation: `3256 ms` for the 3-second fixture grace
- retained transaction directories after the complete success/failure sequence: `2`

Final scenarios proven:

1. a drifted active tag does not replace the actual healthy running image as rollback LKG;
2. source advances only by fast-forward;
3. candidate builds separately and receives the full repository verification gate before successful cutover;
4. successful cutover records distinct source/image identities and reaches healthy runtime readback;
5. explicit rollback restores the retained image/config using local artifacts while the deployment network is internal/offline;
6. injected pre-cutover build failure leaves deployment unchanged;
7. injected post-health runtime-readback failure automatically restores the retained image/config and healthy service;
8. deliberately unhealthy post-cutover candidate automatically restores the retained image/config and healthy service;
9. home/projects/worktrees hashes and ownership remain unchanged through all rollback paths;
10. the runtime failed-candidate marker remains unchanged through deployment rollback;
11. restored deployment retains bounded Docker log settings (`10m`, `3`);
12. retained transaction state remains bounded at two directories.

## Scope / safety readback

- No live `pi-unraid` production service was updated, recreated or rolled back.
- No canonical `/mnt/user/appdata/pi-unraid/home`, `/mnt/user/projects` or `/mnt/user/pi-worktrees` paths were used by verification.
- No OAuth/provider credential or live GitHub credential was consumed by the deployment fixture.
- No Docker socket or host-root mount was introduced into the Pi container.
- Only uniquely named disposable Docker resources and temporary filesystem state were mutated.
- Live on-Unraid deployment/cutover acceptance remains an M03 authorization/readiness obligation.

## Review boundary

M02-T03 requires independent review. This implementing session must not issue the verdict for corrected subject `340121090f4b78ef3d2a3c1377d8bf5477026835`.
