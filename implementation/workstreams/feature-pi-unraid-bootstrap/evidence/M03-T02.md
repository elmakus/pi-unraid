# M03-T02 implementation evidence — upstream Codex-LB dependency cutover

- Card: `M03-T02 — Codex-LB dependency readiness and Pi client boundary`
- Date: 2026-09-23
- Status: **GREEN / READY FOR INDEPENDENT REVIEW**
- Secret handling: no API key, OAuth token, account identifier, encryption-key value or private conversation content is recorded here.

## User authority applied

The user explicitly selected upstream `Soju06/codex-lb` release `v1.25.0-beta.9` for this dependency and stated there is no longer a reason to maintain/deploy the `elmakus/codex-lb` fork.

This is applied as the operator-owned dependency procedure permitted by the approved R2 plan. The Pi access-layer architecture is unchanged.

## Release/image verification

Verified upstream release/image:
- release: `v1.25.0-beta.9`;
- release/image revision: `69f128afcbc616d9f8e924ca6583f7031d75cf82`;
- image: `ghcr.io/soju06/codex-lb:1.25.0-beta.9`;
- image ID: `sha256:867eeb726bf3d8141ed18ac35a827d3a3d0f49e6a6cca285567adf1f98102f0f`;
- repository digest: `sha256:093344377e618acab906a36a4ed23d23e4b2f0fd2882e38df545b170d4278832`.

## Safe cutover

Before the successful cutover:
- the existing `codex-lb-clean` service was stopped cleanly;
- a pre-upgrade snapshot of the live SQLite state and encryption key was created under the existing Codex-LB backup area;
- the previous fork-based container was retained as a stopped rollback candidate.

The first beta.9 start itself reached HTTP 200 health, but an observer-side `jq` formatting error in post-cutover readback triggered the scripted fail-safe. The old database/container were restored and health returned to 200. No application failure caused that rollback.

The second cutover completed GREEN:
- running container: `codex-lb-clean`;
- image: `ghcr.io/soju06/codex-lb:1.25.0-beta.9`;
- restart policy: `unless-stopped`;
- network: `ibraproxy`;
- persistent bind remains `/mnt/user/appdata/codex-lb-clean -> /var/lib/codex-lb`;
- `GET /health` → HTTP 200;
- unauthenticated `GET /v1/models` → HTTP 401;
- Unraid dockerMan template now points to the exact upstream versioned image;
- previous fork-derived deployment remains stopped as a rollback container.

## Persistence and routing readback

After migration and again after an explicit Codex-LB-only restart:
- active ChatGPT/Codex accounts: **3**;
- routing strategy: `capacity_weighted`;
- sticky threads: enabled;
- proxy API-key authentication: enabled;
- pre-existing active API keys: **3**;
- `/health` remained HTTP 200;
- unauthenticated `/v1/models` remained HTTP 401.

No account IDs, e-mail addresses or token values were read into evidence.

## Network-path proof

A disposable container on the ordinary Docker `bridge` network, with Docker `host-gateway` mapping only, reached:

`http://host.docker.internal:2455/health` → HTTP 200.

This proves a Pi-side route can be implemented without joining Codex-LB's `ibraproxy` network and without mounting Codex-LB data. M03-T03 may use this as JIT evidence for the least-coupled provider endpoint design.

## Dedicated Pi client credential

The user manually created a dedicated Codex-LB API key named `pi-unraid-phase1` and stored the one-time plaintext value on Tower at the approved protected host env-file path:

`/mnt/user/appdata/codex-lb-clean/client-secrets/pi-unraid.env`

Secret-safe readback verified:
- file mode: `600`;
- exactly one non-empty `CODEX_LB_API_KEY=...` entry;
- exactly one active Codex-LB key row named `pi-unraid-phase1`;
- authenticated `GET /v1/models` from the Tower host → HTTP 200;
- authenticated `GET /v1/models` from a disposable Docker bridge container through `host-gateway` → HTTP 200;
- returned model payload has a list shape and contained 9 models at readback time.

The secret value was never printed, committed, embedded in an image, or written to evidence.

## Workstation independence readback

After the Codex-LB cutover and credential verification:
- `chatgpt-ce-workstation` remained healthy;
- container ID remained `ba960b239bd29c1908e314ea64b9d44c8883f238feaa4367e6d889227713d5a7`;
- image ID remained `sha256:9ede8f1f522a710707acac32d01fd8c4d7671b91912791e299d1c635d964b893`;
- no project-owned mutation to the workstation was performed.

## Current verdict

The upstream-release deployment, persistence, routing, auth enforcement, dedicated Pi client credential, rollback preservation, non-coupled network route and workstation-independence checks are GREEN. All M03-T02 acceptance items are satisfied. The Card remains non-terminal only because its RECOMMENDED independent review must review the exact persisted subject before terminal `done`.
