# M03-T04 production bootstrap and live functional acceptance evidence

- Card: `M03-T04 — Authorized production bootstrap and live functional acceptance`
- Date: 2026-09-23
- Status: **GREEN implementation/result evidence complete — RECOMMENDED independent review pending**
- Production authorization: `implementation/workstreams/feature-pi-unraid-bootstrap/evidence/M03-T04-authorization.md`
- Production source implemented/deployed at: `a78ea7a7e749bd64c1012b47847680712fb1e3ab`
- Production image: `sha256:ddb23ca173e47cbd77739e6177a5860190221bc9556eeff561e8b2c9c888f005`
- Production mutation: authorized M03-T04 scope only
- Secret handling: no client key or OAuth/token material is recorded in this evidence.

## Pre-write and bootstrap

Fresh Tower readback proved:
- production checkout `/mnt/cachedl/projects/pi-unraid` was clean on `feat/pi-unraid-bootstrap`;
- its prior HEAD was an ancestor of the exact workstream source and fast-forward-only synchronization was legal;
- target ownership remained `99:100`;
- no production Pi container existed before bootstrap;
- Codex-LB `v1.25.0-beta.9` was running healthy and the dedicated Pi client secret existed;
- the live Codex-LB model inventory exposed `gpt-6-sol`;
- `chatgpt-ce-workstation` baseline remained healthy and unchanged.

The checkout was fast-forwarded without reset. Canonical paths were created/reconciled with target ownership:
- `/mnt/user/appdata/pi-unraid/home` → `99:100`;
- `/mnt/user/appdata/pi-unraid/secrets` → `99:100`, restrictive directory permissions;
- existing canonical `/mnt/user/pi-worktrees` symlink resolves to `/mnt/cachedl/pi-worktrees`; its empty target was reconciled to `99:100` without unrelated ownership traversal.

The dedicated existing Pi client credential was copied to `/mnt/user/appdata/pi-unraid/secrets/codex-lb.env` with `99:100 0600`. Compose render proved the secret value was absent from rendered output.

## Target GID correction

The first production start correctly failed closed because Debian already owns GID `100` as group `users`, while the prior entrypoint incorrectly rejected an occupied target GID unless the group was named `pi`.

This was a bounded M03-T04 implementation correction inside PIB-REQ-003:
- `scripts/container-entrypoint.sh` now reuses an existing target group instead of attempting to take over its GID;
- `scripts/verify-compose-foundation.sh` has a regression case proving an existing target GID can be reused while preserving the existing group identity;
- the full Compose foundation regression passed GREEN with the new case `uid=21001,gid=100`.

No strategic authority changed.

## Production runtime and provider

Production then started GREEN with:
- non-root identity `99:100`;
- `restart: unless-stopped`;
- timezone `Europe/Zurich`;
- Pi `0.87.1`;
- persistent provider model `gpt-6-sol`;
- separate `pi-unraid-service health` and `provider-health`, both GREEN;
- Codex-LB base URL through the accepted host-gateway path.

The user-selected first real model interaction was executed through provider `codex-lb` using model `gpt-6-sol` with `thinking=low`. A marker-only response succeeded: `REAL_MODEL_CALL=GREEN`. No direct Pi ChatGPT/Codex OAuth was used and no private model transcript was persisted to evidence.

## Native TUI/session persistence

A native Pi session was created through packaged Pi RPC and opened through a real PTY/TUI path. The session ID was `01a0cede-b93e-75fe-9f70-1268afcf3bca`.

A planned service stop followed by full Compose recreation preserved the persistent session file byte-for-byte. After recreation, packaged Pi reopened the same session file with the same session ID and provider health returned GREEN.

## Projects/worktrees and security

Persistent operator setup wrote the repository's existing Git author identity plus strict GitHub host trust into the Pi home.

A real linked worktree was created from `/projects/pi-unraid` at:
- `/worktrees/m03-t04-live-acceptance`;
- branch `m03-t04-live-acceptance`.

A marker file was written and committed as the non-root Pi user and host ownership read back as `99:100`. This proves canonical repository/worktree read/write and linked-worktree metadata behavior on the production mounts.

Production negative inspection is GREEN:
- expected mounts only, including the read-only dedicated secret;
- no Docker socket;
- no host-root mount;
- no Codex-LB appdata mount;
- container not privileged;
- no published service ports;
- no inbound `sshd`;
- json-file logging remains bounded at `10m × 3`.

## Backup and workstation independence

Appdata Backup config currently includes `/mnt/user/appdata` in `allowedSources`, runs daily, verifies backups by default, and defaults container skip behavior to `no`. There is no `pi-unraid` container override/reference marking it skipped. The new `/mnt/user/appdata/pi-unraid` path is therefore inside the configured normal backup surface and not explicitly excluded.

`chatgpt-ce-workstation` remained exactly on the pre-M03 baseline container/image identity and stayed healthy with `unless-stopped`; Pi work did not mutate or depend on it.

## Authenticated GitHub acceptance and cleanup

After explicit user completion of `gh auth login` in the production Pi home:
- `gh auth status` was GREEN for GitHub account `elmakus`, with Git operations configured for SSH;
- `ssh -T git@github.com` returned GitHub's successful-authentication response under strict host-key verification;
- the prepared local acceptance commit `9e30d1bb8508ee78afb001e541b41e856326ce83` was pushed over SSH to temporary branch `m03-t04-live-acceptance`;
- `git ls-remote` read back the same exact SHA from GitHub;
- a separate temporary Git ref was created with authenticated `gh api`, read back to the same exact SHA, then deleted;
- the SSH-pushed temporary branch was deleted and remote absence was verified;
- the local disposable worktree/branch was removed after the external readbacks.

A final normal Compose recreation then reconfirmed:
- Pi service health GREEN;
- provider health GREEN;
- `gh auth status` remains GREEN from persistent Pi home;
- SSH GitHub authentication remains GREEN from persistent Pi home.

No host/root GitHub credential was copied into Pi.

The former blocker at `implementation/workstreams/feature-pi-unraid-bootstrap/blockers/M03-T04-github-auth.md` is resolved.

## Completion

All M03-T04 acceptance checks are now GREEN on production source `a78ea7a7e749bd64c1012b47847680712fb1e3ab` and image `sha256:ddb23ca173e47cbd77739e6177a5860190221bc9556eeff561e8b2c9c888f005`.

Because the Card classifies independent review as RECOMMENDED, the Card remains non-terminal until a fresh independent reviewer judges this exact implementation/result subject.
