# M03-T05 — Production recovery, restart and final technical handoff

- Milestone: `M03`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#M03 — Codex-LB-integrated on-Unraid acceptance and recoverable handoff`, especially M03-W4 and the integrated verification/recovery matrix
- Requirements: `PIB-REQ-002`, `PIB-REQ-005`, `PIB-REQ-012..024` plus final integrated acceptance of all Definition R2 outcomes
- Accepted decisions: `PIB-ADR-001`, `PIB-ADR-002`, `PIB-ADR-003`, `PIB-ADR-005`
- Relevant OpenSpec: accepted M02 runtime-lifecycle contract plus accepted M03-T03 provider/secret contract
- Accepted dependency results: terminal M03-T04 production result and M01/M02 accepted checkpoints

### Must preserve

- Runtime fallback, deployment rollback, Codex-LB dependency failure and persistent-data damage remain distinct recovery domains.
- Runtime/image rollback never restores Pi home/projects/worktrees or Codex-LB account data backward.
- Planned stop/update retains bounded graceful-stop behavior and persisted sessions remain resumable.
- Codex-LB remains separate and no direct-Pi-OAuth bypass is introduced during dependency failure.
- `chatgpt-ce-workstation` remains independent; disruptive restart evidence must observe its state rather than claim project ownership of it.
- A host/Docker-wide restart occurs only in an explicitly permitted window.
- Final Phase 1 technical acceptance does not add a prescribed coding benchmark or later extension scope.

## Dependencies

- M03-T04 terminal GREEN/accepted production result.

## Outcome

The live Phase 1 deployment demonstrates safe normal update, stable-runtime fallback, actual retained-image rollback, graceful restart/session resume, bounded diagnostics, Codex-LB dependency separation and authorized Docker/host restart recovery; final evidence/handoff covers every Definition R2 acceptance outcome without secret leakage.

## Scope

### Included

- Execute the documented normal update operation against production and verify exact resulting identities/state.
- Exercise failed latest-stable runtime recovery without durable-state loss.
- Exercise actual retained deployment rollback and verify persistent mounts remain unchanged.
- Verify bounded graceful stop and session resume through planned restart/update.
- Verify bounded logging/diagnostics.
- Exercise Codex-LB unavailable/restart/recreation semantics as permitted while preserving independent persistence.
- Observe accepted routing/multi-account behavior and explicitly record continuation-state limitations.
- With separate explicit authorization, perform the required Docker-wide/host restart and verify Pi restart-policy recovery plus Codex-LB/workstation observations.
- Consolidate complete M03 acceptance/recovery/operator handoff, including backup and service-separation evidence.

### Excluded

- Restoring persistent data backward merely to make an old binary work.
- Redesigning Codex-LB routing/account behavior.
- Workstation configuration/runtime changes.
- Later extensions or prescribed real coding benchmark.

## Acceptance

1. Normal production update is executable and healthy with exact source/image/runtime readback.
2. Deliberately failed stable-runtime candidate returns safely to accepted LKG/seed behavior without Pi-home loss.
3. Actual previous deployment rollback works without changing Pi home/projects/worktrees.
4. Planned stop/update proves bounded graceful opportunity and persisted-session resume.
5. Logs remain bounded and diagnostics useful.
6. Codex-LB failure/restart semantics preserve Pi durable state and independent Codex-LB persistence; no direct OAuth fallback appears.
7. Routing/multi-account evidence matches accepted configuration and records the continuation limitation.
8. Authorized Docker/host restart returns Pi according to restart policy and records honest Codex-LB/workstation outcomes.
9. All 21 Definition R2 acceptance outcomes have exact GREEN evidence or the milestone remains visibly incomplete/blocked.
10. Final operator handoff clearly separates Pi runtime, Pi deployment, Codex-LB dependency and persistent-data recovery.
11. Technical acceptance ends with later real-world Pi evaluation remaining user-owned.

## Required tests / checks

- production `scripts/update.sh` normal path and exact readback;
- controlled runtime-candidate failure and LKG/seed recovery;
- retained-image/config rollback with before/after durable-mount hashes/ownership;
- graceful planned stop/session resume;
- sustained/bounded logging readback;
- permitted Codex-LB dependency outage/restart/recreation checks;
- routing/multi-account evidence without account/token disclosure;
- explicitly authorized Docker-wide/host restart and post-restart service/workstation/dependency readback;
- complete Definition R2 requirement/acceptance coverage audit.

## External write/readback needs

Production update/rollback and controlled dependency/restart tests are material external writes. **A separate explicit disruptive-restart authorization is required before Docker-wide/host restart or any similarly broad interruption.** Every operation requires persisted-state readback and sanitized evidence.

## Independent review

`REQUIRED` — this Card performs production update/rollback and disruptive recovery acceptance and closes the technical Phase 1 milestone.
