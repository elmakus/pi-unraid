# M03-T06 — R3 final acceptance reconciliation and handoff

- Milestone: `M03`

> This file is a stable Task Card contract. Mutable execution/review/result state lives only in the selected manifest-bound workstream Task Board.

## Authority slice

- Approved Master Plan: `planning/MASTER_PLAN.md` revision R3, especially M03-W4, complete requirement/acceptance coverage, verification/recovery strategy, authorization gates and downstream reconciliation.
- Requirements: approved `requirements/PI_UNRAID_BOOTSTRAP.md` revision R3 / Definition subject `pi-unraid-bootstrap@R3`.
- Accepted decisions: PIB-ADR-001 through PIB-ADR-007, with PIB-ADR-007 controlling the removed broad-restart acceptance exercise.
- Accepted predecessor results: completed M01/M02 checkpoints; completed M03-T02/M03-T03/M03-T04; completed non-disruptive production-recovery evidence at `implementation/workstreams/feature-pi-unraid-bootstrap/evidence/M03-T05-partial.md`.
- Superseded predecessor contract: `implementation/workstreams/feature-pi-unraid-bootstrap/cards/M03-T05.md` remains historical evidence of the former R2 restart obligation and is not edited in place.

### Must preserve

- No Docker-wide restart, Unraid host restart or similarly broad interruption is performed by this Card.
- Do not claim that broad restart recovery was tested. R3 accepts the residual unverified risk while retaining live readback of `unless-stopped`-style restart policy and `Europe/Zurich`.
- Preserve all valid M03-T05 non-disruptive update, runtime-fallback, retained-image rollback, graceful-stop/session-resume, Codex-LB outage/restart, logging/security and workstation-independence evidence without replay.
- Runtime/image rollback must never roll Pi home, projects, worktrees or Codex-LB account state backward.
- Codex-LB remains separate; no direct-Pi-OAuth bypass and no pooled account token transfer into Pi.
- No secret/client-key/OAuth-token/private-transcript material may be added to evidence.
- Later real-world coding evaluation remains user-owned and outside technical Phase 1 acceptance.

## Dependencies

- `M03-T04` terminal GREEN/accepted production result.
- Existing `M03-T05-partial.md` predecessor evidence is consumed as evidence, not as a dependency on the superseded Card reaching `done`.

## Outcome

Reconcile the stale R2 restart obligation to approved Definition/Plan R3, consolidate the complete Phase 1 technical acceptance and operator-recovery handoff from already-valid evidence, and freeze one exact finalization subject for REQUIRED independent review without any new production/dependency mutation.

## Scope

### Included

- Reconcile Task Board/history so the old M03-T05 Card is superseded only for its obsolete broad-restart acceptance obligation under PIB-ADR-007 while its completed evidence remains referenced.
- Produce finalization evidence that maps all 24 canonical requirements and all 21 Definition R3 acceptance outcomes to exact durable evidence/current proof obligations.
- Record A02/PIB-REQ-013 honestly: accepted restart policy/timezone were read back; broad Docker/host restart was not executed; residual risk is user-accepted.
- Produce/update the M03 operator/recovery handoff, separating Pi runtime failure, Pi deployment failure, Codex-LB dependency failure and persistent-data damage.
- Verify durable repository consistency and secret-safe evidence references.
- Freeze the exact implementation subject for independent review.

### Excluded

- Any Tower, Docker, Codex-LB, workstation, credential or other external live-write.
- Docker-wide/host restart or any replay of already-completed M03-T05 recovery exercises.
- Product/runtime behavior changes, new recovery mechanisms or new extension scope.
- Editing the stable M03-T05 Card contract to pretend it had always been an R3 contract.

## Acceptance

1. Task Board is reconciled to plan revision R3 and M03-T05 is visibly superseded under PIB-ADR-007 without deleting or rewriting its valid partial evidence.
2. Finalization evidence covers PIB-REQ-001..024 and Definition outcomes A01..A21 using exact durable evidence or the explicit R3 proof obligation.
3. PIB-REQ-013/A02 explicitly records `unless-stopped`-style + `Europe/Zurich` configuration/readback, no broad restart execution, and the user-accepted residual risk.
4. Existing M01/M02 and M03-T02/T03/T04/M03-T05 non-disruptive evidence is preserved and not replayed.
5. Operator handoff distinguishes the four recovery domains and does not prescribe a coding benchmark.
6. Evidence contains no secret/client-key/OAuth-token/private-transcript material.
7. No production/dependency live-write occurs during this Card.
8. The exact finalization subject is frozen for REQUIRED independent review.

## Required tests / checks

- repository readback of approved Definition R3, approved Master Plan R3, PIB-ADR-007, Task Board and predecessor evidence pointers;
- mechanical coverage check for all 24 `PIB-REQ-xxx` requirements and A01–A21;
- readback that all referenced predecessor evidence paths exist;
- readback that no broad restart is claimed as executed by finalization evidence/handoff;
- readback that this Card introduced no runtime/deployment/secret behavior change or external live-write;
- diff/scope check limited to R3 reconciliation, evidence/handoff and workflow state.

## External write/readback needs

None. This Card is repository-only reconciliation/finalization. Any newly discovered need for external mutation is outside this Card and must return to the router/gate rather than being performed implicitly.

## Independent review

`REQUIRED` — this Card consolidates the final production acceptance subject and operator handoff for Phase 1 after a material acceptance-boundary change.
