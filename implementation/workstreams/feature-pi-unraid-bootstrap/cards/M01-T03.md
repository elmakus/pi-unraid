# M01-T03 — Native TUI, Git/worktree and operator setup path

- Milestone: `M01`

> Stable Task Card contract. Mutable state lives only in the selected workstream Task Board.

## Authority slice

- Master Plan / milestone contract: `planning/MASTER_PLAN.md#M01 — Reproducible non-root development environment`, especially M01-W3 and section 3.1.
- Requirements: `PIB-REQ-006/007/009/010/012/018` plus applicable global invariants.
- Accepted decisions: `PIB-ADR-001`, `PIB-ADR-002`.
- Relevant OpenSpec: none unless Refresh Gate identifies a material new behavior contract.
- Accepted dependency results: terminal GREEN result of `M01-T02`.

### Must preserve

- Native Pi/TUI path through Docker/Unraid exec with explicit working-directory selection.
- GitHub SSH strict host verification with persistent `known_hosts`; authoritative GitHub keys/fingerprints only.
- Git identity is configurable/persistent; SSH transport and `gh` API auth are separate checks.
- Existing linked worktree metadata must resolve bidirectionally under accepted mounts; do not silently remap or rewrite unrelated repositories.
- Entire persistent home remains in ordinary Unraid appdata backup scope.
- No live OAuth/GitHub credential proof is required at M01 fixture checkpoint.

## Dependencies

- `M01-T02`

## Outcome

Documented native entry and setup path plus disposable Git/worktree fixtures prove explicit cwd, strict GitHub trust setup, persistent operator configuration and accepted workspace semantics.

## Scope

### Included

- Operator documentation/scripts for native shell/TUI entry with explicit cwd.
- Persistent Git identity and SSH known_hosts setup using authoritative GitHub host keys.
- `gh` setup guidance that keeps API auth distinct from SSH transport.
- Disposable canonical repo/worktree fixtures and metadata-resolution checks.
- Backup-scope and runtime-tool-promotion documentation.

### Excluded

- Live ChatGPT OAuth acceptance.
- Destructive rewriting of existing Git worktree metadata.
- M02 updater/shutdown/rollback semantics.
- Later UI/extensions/provider routing.

## Acceptance

- Native service-user environment resolves `pi` and honors explicit selected cwd.
- Disposable repo and linked-worktree fixtures read/write with expected ownership and resolve Git metadata from both directions.
- Strict GitHub host checking is configured from authoritative published keys/fingerprints.
- Git identity and known_hosts persist in fixture home.
- Documentation identifies ordinary appdata backup scope and does not claim fixture tests prove live OAuth/reboot behavior.

## Required tests / checks

- Native exec/TUI command path with explicit `/projects/<repo>` and `/worktrees/<worktree>` fixture cwd.
- Git status/rev-parse/worktree metadata checks from canonical repo and linked worktree.
- SSH configuration/known_hosts strictness inspection against current GitHub published keys/fingerprints.
- Git config persistence and `gh` availability check without persisting secrets in evidence.

## External write/readback needs

Disposable fixture repositories/worktrees/home only on `Tower`. No live credentials required.

## Independent review

`RECOMMENDED` — this closes M01's user-facing access and Git trust/worktree semantics.

## Contract overrides

none
